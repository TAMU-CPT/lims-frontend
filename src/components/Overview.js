import React from 'react';

var Overview = React.createClass({
  getInitialState() {
    return {
      contents: null
    };
  },

  render() {
    return (
            <div>
            </div>
        );
  }
});

export default Overview;
